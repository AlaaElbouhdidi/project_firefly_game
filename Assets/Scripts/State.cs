public enum State {
    Normal,
    Dodging,
    Idle,
    Attacking,
    Blocking
}

public enum EnemyState {
    Idle,
    WalkingToSpawn,
    Attacking,
    Patrol
}

public enum EnemyType {
    Meele,
    Ranged,
    Boss
}